{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run start --concurrency=5",
    "build": "lerna run build",
    "dev": "lerna run dev"
  },
  "devDependencies": {
    "lerna": "^8.1.2",
    "postcss-nesting": "^12.1.0"
  }
}
